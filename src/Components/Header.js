import React, { useState } from 'react';

export default function Header () {
    return (
        <header>
            <img>Logo</img>
            <h1>Tom Gold</h1>
            <Navigation/>
        </header>
    )
}